# MCP Server Module — IBM Bob AI Copilot

Defines the Model Context Protocol (MCP) server integration for IBM Bob.

## Scope (Future Phase)
- Exposes structured operational tools (schedule lookups, prediction explanations, routing breakdowns) to IBM Bob.
- Integrates with FastAPI application services without embedding numerical scheduling logic in the LLM.
